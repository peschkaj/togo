version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "postgres"
        sql_package: "pgx/v4"
        emit_empty_slices: true
        out: "."